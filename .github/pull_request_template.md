## Description
Briefly describe the change and the problem it resolves.

## Invariant Checklist
- [ ] Uses strictly Python 3 standard library (zero new external pip dependencies).
- [ ] Never writes state or metadata inside user project repositories.
- [ ] Preserves Guardrail 3 cleanliness audits (uncommitted work, stashes, unpushed branches).
- [ ] Passes the full isolated acceptance test suite (`python3 tests/test_launchpad.py`).
- [ ] Maintains documentation and clean comments.
